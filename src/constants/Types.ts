export type IconProps = {
    className?: string;
    parentClass?: string;
};

export type Term = {
    title?: string;
    description?: string;
};

export type Policy = {
    title?: string;
    description?: string;
};