function InvalidURL() {
    return (
        <section className="mt-2 flex w-full max-w-3xl items-center justify-center gap-2 bg-white py-5 px-16 shadow rounded">
            <p>Provided url is invalid, retry again.</p>
        </section>
    );
}

export default InvalidURL;